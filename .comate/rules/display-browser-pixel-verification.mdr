---
description: 页面展现相关改动必须用浏览器完成像素级核对后才能交付
alwaysApply: true
---

# Display Browser Pixel Verification

如果改动的功能、代码、数据结构、模板、CSS、资源或构建流程可能影响页面展现，交付前必须使用浏览器相关 skill 完成页面核对。

## 强制要求

- 必须打开受影响页面或最新生成产物。
- 必须使用浏览器 skill 做像素级/视觉级核对，不能只看 HTML 文本、grep、单测或构建结果。
- 必须确认页面展现符合预期后，才能声明完成或交付。
- 如果页面有参考截图、旧版页面或设计目标，必须对照核查布局、卡片层级、文字溢出、重叠、颜色、间距和关键内容是否符合预期。
- 如果浏览器核对无法完成，必须明确说明原因，并不能声称页面展现已经验收通过。

## 适用范围

包括但不限于：

- `v2/display/**`
- `dashboard/**`
- HTML/CSS/JS 模板
- 展示 payload、数据池、卡片字段、区域组合
- 任何会改变页面可见内容或布局的逻辑

## 交付口径

最终回复中要说明：

- 打开的页面或 URL/文件路径
- 使用浏览器核对过的关键区域
- 是否发现展示问题，以及如何处理
